{
  "name": "check-host-monitor-backend",
  "version": "1.0.0",
  "description": "Node.js Backend for Check-Host Monitor",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",  <-- **ต้องมีบรรทัดนี้**
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"  <-- **แนะนำให้ใส่**
  }
}
